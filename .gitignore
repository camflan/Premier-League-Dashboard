node_modules/
.claude/
acronym-generator/
*.local.json
.DS_Store

# Fetcher cache (per-(season,type) hash + timestamp sidecars).
# Not committed — TTL gating is a local-dev convenience; CI re-evaluates from scratch.
data/.cache/
